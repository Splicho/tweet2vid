# X (Twitter) API v2 bearer token — app-only auth. OPTIONAL.
# The app uses the free api.fxtwitter.com by default. Setting a paid
# (Basic tier) token here makes it try the official X API first.
X_BEARER_TOKEN=
